export const environment = {
  // Swapped for environment.development.ts by `ng serve`/`ng build --configuration development`
  // (see angular.json fileReplacements). Set this to the deployed API origin before shipping.
  apiBaseUrl: '',
};
