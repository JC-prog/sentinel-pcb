# Copy to .env and fill in as needed. Empty is fine for the placeholder chat service - it needs
# no secrets. A real LLM-backed ChatService will add its API key here later.

# ui/ (Angular) dev server origin, as a JSON array. Add the deployed origin once one exists.
CORS_ALLOW_ORIGINS=["http://localhost:4200"]
