# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Env / secrets
.env
.env.*
!.env.example

# OS
.DS_Store

# Uploaded chat images (app.settings.chat_upload_dir) and other runtime data - not source.
/data/

# Planning docs (proposals, reports, working design docs) - not part of the shipped repo.
planning/

# Terraform (infra/production/) - local provider cache and any local state/plan output.
# .terraform.lock.hcl (provider version pins) is NOT ignored - that's meant to be committed.
.terraform/
*.tfstate
*.tfstate.*
*.tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Editors
.vscode/
.idea/
